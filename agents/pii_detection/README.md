# PII Detection Agent (planned)

Will reuse detector contracts from `shared/guardrails` and return only locations, classifications, and confidence scores. Original values will be opt-in and disabled by default.
